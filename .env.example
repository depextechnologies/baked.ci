# Copy to .env at the repo root (used by docker-compose.yml at build time).
# Must match REACT_APP_BACKEND_URL set in backend/.env.
REACT_APP_BACKEND_URL=https://your-domain.com

# nginx server_name inside the frontend container (space-separated for
# multiple domains). Local dev: localhost (or leave unset). Production:
# SERVER_NAME=baked.ci www.baked.ci
SERVER_NAME=localhost

# Google Maps JS API key for the address picker / Express live tracking maps.
# Leave blank to show the "map unavailable" fallback instead.
REACT_APP_GOOGLE_MAPS_API_KEY=

# Must match the values in backend/.env — docker-compose.yml interpolates
# these for the postgres service and the backend's DATABASE_URL, and Compose
# only reads the root .env for that, not backend/.env.
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
